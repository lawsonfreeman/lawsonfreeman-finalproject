{% extends '_templates/base.njk' %}

{% set relative_path = "../" %}

{% block page_title %}{{ name }}{% endblock %}

{% block page_description %}{{ name }}{% endblock %}

{% block content %}
  <article class="container">
    <div class="row">
      <div class="col-md-4">
          <img src="../img/img-queen/members/{{ slug }}-color.jpg" alt="" class="img-fluid mb-4">
      </div>
      <div class="col-md-8">
          <h1>{{ name }}</h1>
          <p>Member from: {{ tenure }}</p>
          <p>Birthdate: {{ birthdate }}</p>
          <p>Birthplaces: {{ birthplace }}</p>
          <p>{{ description }}</p>
      </div>
    </div>
  </article>
{% endblock %}
